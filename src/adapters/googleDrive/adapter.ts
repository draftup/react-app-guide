import { putTaskToLocalStorage } from "./methods/putTask";

export const googleDriveServiceAdapter = (operations: Operation[]) => {

    // operations.forEach((operation) => {
    //     // dispatch(modifyOperation(operation).setStatus('IN_PROCESS').done());

    //     switch(operation.name) {
    //         case 'PUT_TASK': {
    //             putTaskToLocalStorage();
    //             dispatch(modifyOperation(operation).setStatus('SUCCEEDED').setValue())
    //         }
            
    //     }
    // })

}